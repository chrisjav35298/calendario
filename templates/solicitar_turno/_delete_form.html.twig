<form method="post" action="{{ path('app_solicitar_turno_delete', {'id': solicitar_turno.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ solicitar_turno.id) }}">
    <button type="submit" class="btn btn-primary bg-red-600 hover:bg-red-800 text-white font-bold my-4 mx-9 py-2 px-4 text-align: right; focus:outline-none focus:shadow-outline">
Borrar</button>
</form>
