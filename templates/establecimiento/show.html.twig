{% extends 'base.html.twig' %}

{% block title %}Establecimiento{% endblock %}

{% block body %}
    <h1>Establecimiento</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ establecimiento.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ establecimiento.nombre }}</td>
            </tr>
            <tr>
                <th>Cueanexo</th>
                <td>{{ establecimiento.cueanexo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_establecimiento_index') }}">back to list</a>

    <a href="{{ path('app_establecimiento_edit', {'id': establecimiento.id}) }}">edit</a>

    {{ include('establecimiento/_delete_form.html.twig') }}
{% endblock %}
